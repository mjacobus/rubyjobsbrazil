<%= simple_form_for(job) do |f| %>
  <%= f.input :title %>
  <%= f.input :description, as: :text, input_html: { rows: 5 } %>
  <%= f.input :how_to_apply, as: :text, input_html: { rows: 5 } %>

  <%= f.submit t('system.links.forms.save') %>
<% end %>
