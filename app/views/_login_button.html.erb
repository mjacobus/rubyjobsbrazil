<%
options ||= {}

default_options  = {
  class: "login #{provider}",
  title: t("recruiter.messages.login_with.#{provider}")
}

options.reverse_merge!(default_options)
%>

<%= link_to recruiter.user_omniauth_authorize_path(provider), options do %>
  <%= provider_icon(provider) %>
  <%= t("recruiter.links.login.#{provider}") %>
<% end %>
