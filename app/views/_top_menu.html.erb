<div class="sticky">
  <nav class="top-bar" data-topbar>
    <ul class="title-area">
      <li class="name"><h1><a href="/"><%= t('app.site_title') %></a></h1> </li>
      <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
    </ul>


    <section class="top-bar-section">
      <!-- Right Nav Section -->
      <ul class="right">
        <li> <%= link_to(t('app.links.articles.index'), articles_path) %> </li>
        <li> <%= link_to(t('app.links.articles.mine'), user_articles_path) if current_user %> </li>
        <li> <%= link_to(t('app.links.jobs.new'), new_user_job_path) %> </li>
        <%= render '/login_links' unless current_user -%>
        <%= render '/login_status' if current_user -%>
      </ul>
    </section>
  </nav>
</div>

<div id="after_menu">
  <div class="row">
    <div class="small-12 columns">
      <div class="wrapper">
        <%= render '/title_header' %>
      </div>
    </div>
  </div>
</div>
