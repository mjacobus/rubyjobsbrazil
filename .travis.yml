language: ruby
sudo: false
cache: bundler

rvm:
  - 2.5.1

env:
  - DB=mysql

script:
  - RAILS_ENV=test ./bin/rake db:migrate --trace
  - ./bin/rspec spec/

before_script:
  - mysql -e 'create database RubyJobsBrazil_test'
