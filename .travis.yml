language: ruby
sudo: false
# cache: bundler

env:
  - DB=mysql

script:
  - gem install bundler '1.16.1'
  - RAILS_ENV=test ./bin/rake travis_ci
  # - ./bin/rspec --exclude-pattern "spec/{features}/**/*_spec.rb"
  # - SKIP_COVERAGE=true xvfb-run --server-args="-screen 0 1024x768x24" ./bin/rspec spec/features

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  # - mysql -e 'create database RubyJobs_test'
