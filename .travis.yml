language: ruby
sudo: false
# cache: bundler

env:
  - DB=mysql

# rvm:
#   - 2.5.1

script:
  - gem install bundler '1.16.1'
  - RAILS_ENV=test ./bin/rake db:migrate
  - ./bin/rspec
  # - xvfb-run --server-args="-screen 0 1024x768x24" ./bin/rspec spec/features

before_script:
  - mysql -e 'create database RubyJobs_test'
